# 工程名称
workspace "SHNineGrid.xcworkspace"
# App工程名称
xcodeproj "SHNineGrid.xcodeproj"

# 最低要求版本
platform :ios, "8.0"
# 去除pod库警告
#inhibit_all_warnings!

# use_frameworks!

# pod库只存在当前目标
# pod库只存在当前目标
target :"SHNineGrid" do
pod 'SDWebImage', '~> 4.3.3'
pod 'Masonry', '~> 1.1.0'
end

